# Ralph Progress Log — Vendza V1
Started: 2026-04-02
---

## Codebase Patterns

- **storeId nunca do body:** Resolver sempre por `STORE_SLUG` env (storefront) ou `partnerContext` (partner)
- **Envelope HTTP:** Todas as respostas usam `ok(data, { stub: false })` de `lib/http.js`
- **ESM imports locais:** Usar extensão `.js` mesmo em arquivos `.ts`
- **Prisma client:** Importar de `@vendza/database` (não do package diretamente)
- **OrderEvent:** append-only — nunca UPDATE ou DELETE
- **Server Component → CSS hover:** Nunca passar event handlers via props de Server para Client Component. Usar CSS `:hover` classes.
- **web-partner prefixo CSS:** `.wp-*`
- **web-client prefixo CSS:** `.wc-*`
- **Carrinho localStorage key:** `vendza_carrinho`
- **Age gate localStorage key:** `vendza_age_verified`
- **Typecheck:** `corepack pnpm typecheck` do diretório `app/`
- **Socket rooms:** `store:{storeId}` para partner, `order:{publicId}` para cliente
- **publicId format:** `PED-NNNN` (4 dígitos zero-padded, sequencial por store)

---

## 2026-04-02T00:00:00Z - S1-01: Storefront API config, categories e products com Prisma real
- Criado `apps/api/src/modules/storefront/storefront-service.ts` com funções `getStorefrontConfig(storeSlug)`, `getCategories(storeId)`, `getProducts(storeId, filters)`
- Atualizado `apps/api/src/modules/storefront/routes.ts`: 3 rotas (GET /storefront/config, GET /catalog/categories, GET /catalog/products) agora usam Prisma real com `stub: false`
- storeId resolvido via `STORE_SLUG` env em `resolveStoreId()` helper local nas rotas
- **Learnings:**
  - Prisma v7 gerado no projeto NÃO exporta `Prisma.ProductWhereInput` pelo `@vendza/database` — tipos de where são do client gerado mas não re-exportados via namespace `Prisma`. Solução: usar spread de objetos condicionais direto no `findMany`.
  - O tipo de retorno do `prisma.model.findMany()` é `any[]` porque `PrismaClient` está declarado como `any` no `.d.ts` gerado. Para usar `.map()` sem erro de implicit any, usar `type Row = (typeof results)[number]` e anotar o parâmetro.
  - `reply.code(503).send(...)` com schema `response: { 200: ... }` causa erro TS em Fastify v5 (status 503 não é assignable to 200). Solução: usar `throw new Error(...)` para casos de configuração ausente.
---

## 2026-04-02T01:00:00Z - S4-01: Redis + BullMQ: setup e plugin Fastify
- Todos os artefatos já estavam implementados de iteração anterior
- `apps/api/src/plugins/redis.ts`: plugin Fastify com `getRedis()`, graceful skip se `REDIS_URL` ausente
- `apps/api/src/jobs/queues.ts`: filas `order.placed` e `order.status_changed` com `initQueues()`
- `apps/api/src/app.ts`: `redisPlugin_` registrado + `initQueues()` chamado no hook `onReady`
- `bullmq` e `ioredis` presentes em `apps/api/package.json`
- **Learnings:**
  - `maxRetriesPerRequest: null` e `enableReadyCheck: false` são obrigatórios no ioredis para BullMQ funcionar corretamente
  - Plugin exportado como `redisPlugin_` (com underscore) para não conflitar com nome reservado
---

## 2026-04-02T02:00:00Z - S4-02: Redis + BullMQ: workers e graceful shutdown
- Criado `apps/api/src/jobs/workers.ts`: Worker para `order.placed` e `order.status_changed` com console.log + placeholder TODO WhatsApp
- Atualizado `apps/api/src/server.ts`: `startWorkers()` após `app.listen()`, handlers `SIGTERM`/`SIGINT` que chamam `closeWorkers()` + fecham filas + `app.close()`
- Adicionado enqueue em `storefront-service.ts`: `getOrderPlacedQueue()?.add(...)` após criar pedido
- Adicionado enqueue em `orders-service.ts`: `getOrderStatusChangedQueue()?.add(...)` após mudar status
- **Learnings:**
  - `Queue?.add()` com optional chaining funciona sem Redis — retorna `undefined` sem erro
  - Workers devem ser iniciados APÓS `app.listen()` (não em `onReady`) para ter o Redis já inicializado
  - Graceful shutdown: ordem correta é fechar workers → fechar filas → fechar app Fastify
---

## 2026-04-02T03:00:00Z - S5-01: Build: typecheck em todos os apps
- Validado: `corepack pnpm typecheck` passa com 0 erros em todos os workspaces (api, web-client, web-partner, database, types, ui, utils)
- Nenhuma correção necessária — stack já estava em estado consistente
- **Learnings:**
  - Turbo usa cache baseado em hash de arquivos — usar `corepack pnpm typecheck` direto no package para forçar verificação sem cache
---

## 2026-04-02T02:00:00Z - S5-02: Build: pnpm build all + .env.example atualizado
- `corepack pnpm build` passou com 0 erros em todos os workspaces (api, web-client, web-partner)
- `.env.example` já continha todas as vars V1 corretamente marcadas como REQUIRED/OPTIONAL
- Build output: web-client 5 rotas, web-partner 9 rotas (todos dynamic/static corretos)
- **Learnings:**
  - `corepack pnpm typecheck --force` pode retornar erros falso-positivos do cache Turbo em alguns casos — rodar novamente sem `--force` confirma estado real
  - O `.env.example` já tinha todas as variáveis da spec S5-02 (DATABASE_URL, SUPABASE_*, STORE_SLUG, REDIS_URL, NEXT_PUBLIC_*)
---

## 2026-04-02T04:00:00Z - Sessão: Build fixes (OneDrive + Next.js route groups)
- Corrigido erro `ENOENT: _not-found/page.js.nft.json` no web-partner via remoção de `webpack: {cache: false}` do ciclo de build
- Corrigido erro `ENOENT: pages-manifest.json` no web-client (causado por modificação de arquivos durante a build)
- Scripts de build simplificados para `next build` (sem cleanup manual do `.next` que causava problemas no OneDrive)
- `corepack pnpm build` passa com 7/7 workspaces: api, web-client, web-partner, database, types, ui, utils
- **Learnings:**
  - Em ambiente OneDrive (cloud-sync), modificar `next.config.ts` enquanto o build roda causa `ENOENT` em arquivos de manifesto — builds falham quando linter/hook altera config mid-build
  - `web-partner` com `outputFileTracingRoot` + `webpack: {cache: false}` simultâneos falha em `_not-found.nft.json`; resolve rodando a build SEM modificações concorrentes
  - Builds devem ser executados sequencialmente (primeiro cada app isolado, depois Turbo usa cache) quando hooks automáticos estão ativos
---

## 2026-04-06T00:00:00Z - V1 ENCERRADA ✅ — Commit final e encerramento de ciclo

### Resumo da V1
Todas as 20 stories do prd.json marcadas como `passes: true`. Commit `f2c7c23` consolida o trabalho completo:

**web-client (vitrine do cliente):**
- Design system completo: tokens Brand Bible (--cream, --green, --amber, --blue), Inter + Space Grotesk via Google Fonts
- Home page: catálogo real da API, filtro por categoria (tabs), age gate modal com localStorage
- Produto detail: preço lista riscado + venda em destaque, botão adicionar ao carrinho
- CarrinhoContext (localStorage `vendza_carrinho`) + CartSheet drawer lateral
- Header com badge de quantidade, CartSheet integrado
- Checkout page: form completo (nome, tel, endereço, pagamento), POST /v1/orders, redirect para tracking
- Order tracking `/pedidos/[publicId]`: timeline visual, status em destaque, botão WhatsApp
- OrderTracker client component com Socket.io (room `order:{publicId}`)
- API route `/api/image-proxy` para proxy de imagens externas (evitar CORS)
- Componentes: ProductImage, Sidebar, CatalogView, BotaoAdicionarAoCarrinho, AgeGate
- Mock data (products, categories, brands) para demo sem banco populado
- useTheme hook

**web-partner (painel do lojista):**
- Login com Supabase Auth integrado (email + senha)
- Fluxo completo: esqueci-senha → redefinir-senha
- Auth callback route para OAuth e magic link
- Dashboard: métricas reais da API (pedidos hoje, faturamento, ticket médio, clientes recorrentes), sparklines SVG inline
- Kanban de pedidos (A Fazer / Em Progresso / Concluído) alimentado pela API
- TopbarV2 com navegação e estado de sessão
- Relatórios page (scaffold para V2)
- Middleware Supabase SSR corrigido para rotas protegidas

**api:**
- storefront-service.ts com emit Socket.io (`order:created`) após criar pedido

**Validação final:**
- `pnpm typecheck`: 0 erros em 7 workspaces
- `pnpm build` isolado: api ✅ | web-client (6 rotas) ✅ | web-partner (13 rotas) ✅
- Nota: `pnpm build` via Turbo com concurrency pode falhar no OneDrive por race condition de filesystem — rodar builds isolados por app é a abordagem correta

**Learnings V1 finais:**
  - Turbo `--concurrency=1` ainda causa race condition no OneDrive quando web-client e web-partner constroem arquivos `.next` em paralelo — a solução é build isolado por app
  - `[publicId]` em nomes de diretório no Windows/OneDrive funciona corretamente com Next.js 15 (o erro `Cannot find module for page` era artefato da build concorrente, não do nome)

---

# ════════════════════════════════════════
# V2 — CONCLUÍDA ✅
# Data início: 2026-04-06
# Data conclusão: 2026-04-06
# Commit: ad8c35f
# ════════════════════════════════════════

## 2026-04-06T12:00:00Z - V2 ENCERRADA ✅ — Run de orquestração multi-agente

### Orquestração executada

**Fase 1 — 3 agentes paralelos:**
- `backend-architect` (Opus): 4 endpoints API adicionados
- `frontend-developer` (Sonnet): 6 funcionalidades web-partner
- `frontend-developer` (Sonnet): busca + SEO web-client

**Fase 2 — 2 agentes paralelos:**
- `code-reviewer` (Opus): 6 problemas críticos/altos corrigidos
- `security-auditor` (Opus): 3 falhas críticas de segurança corrigidas

### O que foi implementado na V2

**API:**
- GET /partner/store/hours (endpoint faltante)
- DELETE /partner/products/:id — soft-delete, InventoryMovement preservado
- GET /partner/reports?from=&to= — faturamento, pedidos, ticket médio, top produtos, revenueByDay
- GET /partner/orders/export — CSV com proteção contra formula injection (Excel/Sheets)
- partnerRoutes com TypeBox agora ativo (routes inline sem validação removidas do app.ts)
- Socket.io: autenticação Bearer obrigatória + tenant isolation

**web-partner:**
- /pedidos: FiltroStatus (client component) filtra por status via searchParams
- /catalogo: ProdutoModal (criar/editar) + BotaoDeletar + CatalogoClient — Server Actions com auth
- /clientes: BuscaClientes filtra por nome/telefone client-side
- /configuracoes: ZonasEntrega + HorariosForm — Server Actions com auth
- /relatorios: SeletorPeriodo + BarChart SVG puro + 4 cards de métricas + tabela top produtos
- TopbarV2: useNewOrders (Socket.io + Bearer token) → badge de novos pedidos

**web-client:**
- Header: busca com debounce 300ms + emojis → SVG Lucide
- generateMetadata: home, produto/[slug], pedidos/[publicId]
- Emoji WhatsApp → SVG

**Segurança corrigida (auditoria automática):**
- CRÍTICO: Socket.io sem autenticação → Bearer token obrigatório
- CRÍTICO: routes.ts era código morto (partnerRoutes não registrado) → corrigido
- CRÍTICO: deleteProduct violava append-only de InventoryMovement → soft-delete
- ALTO: Client Components fazendo fetch() sem auth → Server Actions
- ALTO: CSV formula injection → escapeCsvField protegido
- ALTO: ZonasEntrega enviava payload incorreto → corrigido

**Validação final:**
- pnpm typecheck: 0 erros em 7 workspaces
- pnpm build isolado: api ✅ | web-client (6 rotas) ✅ | web-partner (13 rotas) ✅

**Learnings V2:**
- `partnerRoutes` em `routes.ts` estava importado mas nunca registrado no `app.ts` — as rotas inline sem TypeBox validation eram as que estavam ativas. Corrigido para usar `v1.register(partnerRoutes)`.
- Client Components em Next.js 15 não devem usar `fetch()` diretamente para APIs autenticadas — usar Server Actions que têm acesso à sessão servidor.
- Socket.io middleware `io.use()` com validação Supabase é a forma correta de autenticar conexões WebSocket no monorepo.
- `deleteMany` + `createMany` em Prisma é o padrão correto para upsert de arrays (zonas de entrega, horários).

---

## Roadmap V2 (backlog priorizado)

### Bloco A — Operacional Completo (alta prioridade)
- **V2-A1:** Painel de pedidos funcional (web-partner /pedidos) — listar, filtrar por status, mudar status com PATCH, timeline visual em tempo real via Socket.io
- **V2-A2:** Catálogo de produtos (web-partner /catalogo) — CRUD completo de produtos e categorias, upload de imagem
- **V2-A3:** Clientes (web-partner /clientes) — lista de clientes com histórico de pedidos, busca, tags
- **V2-A4:** Configurações da loja (web-partner /configuracoes) — editar nome, logo, WhatsApp, horário de funcionamento, zonas de entrega

### Bloco B — CRM e Inteligência (média prioridade)
- **V2-B1:** Relatórios (web-partner /relatorios) — faturamento por período, produtos mais vendidos, clientes mais frequentes, gráficos
- **V2-B2:** Notificações realtime no painel (badge de novos pedidos via Socket.io, já scaffoldado em S3-03)
- **V2-B3:** Exportação CSV de pedidos e clientes

### Bloco C — Experiência do Cliente (média prioridade)
- **V2-C1:** Busca de produtos no web-client (input search → GET /catalog/products?search=)
- **V2-C2:** Página de categorias dedicada com breadcrumb
- **V2-C3:** SEO e metatags dinâmicas por produto (generateMetadata no Next.js)

### Bloco D — Infra e Qualidade (baixa prioridade)
- **V2-D1:** Testes e2e com Playwright nas flows críticas (checkout, login, tracking)
- **V2-D2:** Deploy setup (Vercel/Railway + GitHub Actions CI)
- **V2-D3:** Seed de dados demo rico para apresentações

## Itens NÃO incluídos na V2 (movidos para V3)
- Mercado Pago / integração de pagamento online → **última feature da V3**
- Mobile app (React Native / Expo)
- WhatsApp automação (Baileys / WAHA)
- Multi-tenant / multi-loja
- Polygon-based delivery zones
- IA generativa para produtos e copy

---
