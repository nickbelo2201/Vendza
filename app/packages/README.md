# Packages

Esta pasta guarda tudo o que precisa ser compartilhado:

- `database`: schema, migrations, seeds e cliente ORM
- `ui`: design system e componentes compartilhados
- `types`: enums, DTOs, contratos e eventos
- `utils`: formatacao, validacoes e helpers
